{% extends '@App/base.html.twig' %}
 {% block content %}
     <div class="container">

         <div class="starter-template">
             <h1>Twój drink na dziś to:
                 <div>
                     <strong>
                         {{ drink.title }}
                     </strong>
                 </div>
             </h1>
             <img src="{{ drink.image }}"/>

             <p>
                 {{ drink.description }}
             </p>

             <div>
                 <a href="{{ path('random') }}" class="btn btn-default btn-lg btn-block">Spróbuj raz jeszcze - Losuj</a>
             </div>
         </div>

     </div>
 {% endblock %}
